import React from 'react';

const WasteCard = ({ title, body }) => {
  return (
    <div>
      <div>
        <p>{title}</p>
        <p>{body}</p>
      </div>
    </div>
  );
}

export default WasteCard; 